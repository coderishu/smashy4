import { BenefitCategoryPipe } from './benefit-category.pipe';

describe('BenefitCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new BenefitCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
