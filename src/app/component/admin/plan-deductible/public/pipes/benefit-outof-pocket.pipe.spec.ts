import { BenefitOutofPocketPipe } from './benefit-outof-pocket.pipe';

describe('BenefitOutofPocketPipe', () => {
  it('create an instance', () => {
    const pipe = new BenefitOutofPocketPipe();
    expect(pipe).toBeTruthy();
  });
});
